<?php
/**
 * Cart empty state — reference-faithful.
 * Keeps the 2-column cart shell even when there are no items.
 *
 * @var \Magento\Checkout\Block\Cart\Crosssell $block
 * @var \Magento\Framework\Escaper $escaper
 */
$priceHelper = $this->helper(Magento\Framework\Pricing\Helper\Data::class);
?>
<div class="cart-container pvcart-empty-layout">
    <div class="pvcart-list-card">
        <div class="pvcart-empty">
            <div class="pvcart-empty-icon">🛒</div>
            <h3 class="pvcart-empty-title"><?= $escaper->escapeHtml(__('Giỏ hàng trống')) ?></h3>
            <p class="pvcart-empty-sub"><?= $escaper->escapeHtml(__('Thêm sản phẩm vào giỏ hàng để tiếp tục mua sắm')) ?></p>
            <a class="pvcart-empty-btn"
               href="<?= $escaper->escapeUrl($block->getUrl('')) ?>">
                <?= $escaper->escapeHtml(__('Tiếp tục mua sắm')) ?>
            </a>
        </div>
    </div>

    <aside class="pvcart-summary-card pvcart-summary-card--empty" aria-label="<?= $escaper->escapeHtmlAttr(__('Tóm tắt đơn hàng')) ?>">
        <div class="pvcart-summary-head">
            <h2>
                <span><?= $escaper->escapeHtml(__('Tóm tắt đơn hàng')) ?></span>
                <span class="pvcart-summary-count">0</span>
            </h2>
        </div>

        <div class="pvcart-summary-breakdown">
            <div class="pvcart-summary-row">
                <span><?= $escaper->escapeHtml(__('Tạm tính (0 sản phẩm)')) ?></span>
                <strong><?= /* @noEscape */ $priceHelper->currency(0, true, false) ?></strong>
            </div>
            <div class="pvcart-summary-row">
                <span><?= $escaper->escapeHtml(__('Phí vận chuyển')) ?></span>
                <strong><?= /* @noEscape */ $priceHelper->currency(0, true, false) ?></strong>
            </div>
        </div>

        <div class="pvcart-summary-total">
            <div class="pvcart-summary-row pvcart-summary-row--total">
                <span><?= $escaper->escapeHtml(__('Tổng cộng')) ?></span>
                <strong><?= /* @noEscape */ $priceHelper->currency(0, true, false) ?></strong>
            </div>
        </div>

        <div class="pvcart-summary-actions">
            <button type="button"
                    class="pvcart-primary-btn is-disabled"
                    disabled="disabled"
                    aria-disabled="true">
                <span><?= $escaper->escapeHtml(__('Tiếp tục thanh toán')) ?></span>
            </button>
            <a class="pvcart-secondary-btn"
               href="<?= $escaper->escapeUrl($block->getUrl('')) ?>">
                <?= $escaper->escapeHtml(__('Tiếp tục mua sắm')) ?>
            </a>
        </div>
    </aside>
</div>
